<Window x:Class="mamaRabota.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:mamaRabota"
        mc:Ignorable="d"
        Title="Активация программы" 
        Height="450" Width="800"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F0F4F8">
    <!-- Светло-серый фон окна -->

    <Window.Resources>
        <!-- Стиль для кнопки -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#4A90E2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                Padding="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#357ABD"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2A5F9E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для TextBox -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="50"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D1D9E6"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#4A90E2"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#7BA7D9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Центральная карточка -->
        <Border Background="White" 
                CornerRadius="15" 
                Width="450" 
                Height="320"
                Margin="0,20,0,0">
            <!-- Тень для карточки -->
            <Border.Effect>
                <DropShadowEffect Color="#000000" Direction="270" ShadowDepth="2" Opacity="0.15" BlurRadius="15"/>
            </Border.Effect>

            <StackPanel Margin="40,30,40,30" VerticalAlignment="Center">

                <!-- Иконка замка или ключа (символ) -->
                <TextBlock Text="🔑" FontSize="40" HorizontalAlignment="Center" Margin="0,0,0,10"/>

                <!-- Заголовок -->
                <TextBlock Text="Активация лицензии" 
                           FontSize="24" 
                           FontWeight="Bold" 
                           Foreground="#2C3E50" 
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,5"/>

                <TextBlock Text="Введите ключ, полученный при покупке" 
                           FontSize="13" 
                           Foreground="#7F8C8D" 
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,25"/>

                <!-- Поле ввода -->
                <TextBox x:Name="txtKey" 
                         Style="{StaticResource ModernTextBox}" 
                         Margin="0,0,0,25"
                         MaxLength="50"/>

                <!-- Кнопка -->
                <Button Content="Активировать" 
                        Name="enterBtn" 
                        Style="{StaticResource PrimaryButton}" 
                        Height="50" 
                        Click="enterBtn_Click"/>
            </StackPanel>
        </Border>

        <!-- Версия программы внизу (опционально) -->
        <TextBlock Text="v1.0.0" 
                   Foreground="#95A5A6" 
                   FontSize="10" 
                   HorizontalAlignment="Right" 
                   VerticalAlignment="Bottom" 
                   Margin="0,0,15,10"/>
    </Grid>
</Window>